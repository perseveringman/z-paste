# stash 文档站 Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** 在 `website/` 子目录创建 Docusaurus 中文文档站，包含产品首页、安装/功能指南、FAQ、更新日志，并通过 GitHub Actions 自动部署到 GitHub Pages。

**Architecture:** 在项目根目录的 `website/` 下初始化 Docusaurus Classic 主题项目，独立 `package.json`；GitHub Actions workflow（`docs.yml`）监听 `website/` 目录改动，构建后推送到 `gh-pages` 分支供 GitHub Pages 托管。

**Tech Stack:** Docusaurus 3、React 18、TypeScript、GitHub Actions (`peaceiris/actions-gh-pages`)

---

## 任务总览

1. 初始化 Docusaurus 项目骨架
2. 配置 `docusaurus.config.ts`（站点元数据、导航、页脚、主题）
3. 配置侧边栏 `sidebars.ts`
4. 编写产品首页 `src/pages/index.tsx`
5. 编写文档页：安装指南
6. 编写文档页：剪切板管理指南
7. 编写文档页：密码保险箱指南
8. 编写文档页：快捷键参考
9. 编写文档页：FAQ
10. 编写文档页：更新日志
11. 创建 GitHub Actions 部署 workflow
12. 本地构建验证 + 提交

---

### Task 1: 初始化 Docusaurus 项目骨架

**Files:**
- Create: `website/` 目录（通过 `create docusaurus` 脚手架）

**Step 1: 在项目根目录运行脚手架**

```bash
cd /path/to/z-paste
npx create-docusaurus@latest website classic --typescript --skip-install
```

说明：`--skip-install` 避免自动装包（我们手动装以便确认版本）。这会生成完整骨架，包含 `docusaurus.config.ts`、`sidebars.ts`、`src/pages/`、`docs/` 等。

**Step 2: 安装依赖**

```bash
cd website && npm install
```

**Step 3: 验证本地启动正常**

```bash
npm run start
```

浏览器打开 `http://localhost:3000` 应显示默认 Docusaurus 页面。

**Step 4: 停止开发服务器（Ctrl+C），提交骨架**

```bash
cd ..  # 回到项目根
git add website/
git commit -m "feat(docs): initialize Docusaurus website scaffold"
```

---

### Task 2: 配置 `docusaurus.config.ts`

**Files:**
- Modify: `website/docusaurus.config.ts`（完整替换）

**Step 1: 替换 `website/docusaurus.config.ts` 为以下内容**

```typescript
import { themes as prismThemes } from 'prism-react-renderer'
import type { Config } from '@docusaurus/types'
import type * as Preset from '@docusaurus/preset-classic'

const config: Config = {
  title: 'Stash',
  tagline: 'macOS 剪切板管理 + 密码保险箱',
  favicon: 'img/favicon.ico',

  url: 'https://perseveringman.github.io',
  baseUrl: '/z-paste/',

  organizationName: 'perseveringman',
  projectName: 'z-paste',
  trailingSlash: false,

  onBrokenLinks: 'throw',
  onBrokenMarkdownLinks: 'warn',

  i18n: {
    defaultLocale: 'zh-Hans',
    locales: ['zh-Hans'],
  },

  presets: [
    [
      'classic',
      {
        docs: {
          sidebarPath: './sidebars.ts',
        },
        blog: false,
        theme: {
          customCss: './src/css/custom.css',
        },
      } satisfies Preset.Options,
    ],
  ],

  themeConfig: {
    colorMode: {
      defaultMode: 'dark',
      disableSwitch: false,
      respectPrefersColorScheme: true,
    },
    navbar: {
      title: 'Stash',
      logo: {
        alt: 'Stash Logo',
        src: 'img/logo.png',
      },
      items: [
        {
          type: 'docSidebar',
          sidebarId: 'docsSidebar',
          position: 'left',
          label: '使用文档',
        },
        {
          href: 'https://github.com/perseveringman/Stash',
          label: 'GitHub',
          position: 'right',
        },
        {
          href: 'https://github.com/perseveringman/Stash/releases/latest',
          label: '下载',
          position: 'right',
        },
      ],
    },
    footer: {
      style: 'dark',
      links: [
        {
          title: '文档',
          items: [
            { label: '安装指南', to: '/docs/installation' },
            { label: '剪切板管理', to: '/docs/clipboard' },
            { label: '密码保险箱', to: '/docs/vault' },
          ],
        },
        {
          title: '更多',
          items: [
            { label: 'GitHub', href: 'https://github.com/perseveringman/Stash' },
            { label: '反馈问题', href: 'https://github.com/perseveringman/Stash/issues' },
          ],
        },
      ],
      copyright: `Copyright © ${new Date().getFullYear()} Stash. Built with Docusaurus.`,
    },
    prism: {
      theme: prismThemes.github,
      darkTheme: prismThemes.dracula,
    },
  } satisfies Preset.ThemeConfig,
}

export default config
```

**Step 2: 验证配置无报错**

```bash
cd website && npm run build 2>&1 | head -30
```

预期：看到构建进度，没有 Error。

**Step 3: 提交**

```bash
cd ..
git add website/docusaurus.config.ts
git commit -m "feat(docs): configure Docusaurus site metadata and navigation"
```

---

### Task 3: 配置侧边栏 `sidebars.ts`

**Files:**
- Modify: `website/sidebars.ts`（完整替换）

**Step 1: 替换内容**

```typescript
import type { SidebarsConfig } from '@docusaurus/plugin-content-docs'

const sidebars: SidebarsConfig = {
  docsSidebar: [
    {
      type: 'doc',
      id: 'installation',
      label: '安装指南',
    },
    {
      type: 'category',
      label: '功能指南',
      items: ['clipboard', 'vault'],
    },
    {
      type: 'doc',
      id: 'shortcuts',
      label: '快捷键参考',
    },
    {
      type: 'doc',
      id: 'faq',
      label: 'FAQ',
    },
    {
      type: 'doc',
      id: 'changelog',
      label: '更新日志',
    },
  ],
}

export default sidebars
```

**Step 2: 提交**

```bash
cd ..
git add website/sidebars.ts
git commit -m "feat(docs): configure sidebar navigation structure"
```

---

### Task 4: 编写产品首页 `src/pages/index.tsx`

**Files:**
- Modify: `website/src/pages/index.tsx`（完整替换）
- Modify: `website/src/css/custom.css`（追加样式）

**Step 1: 替换 `website/src/pages/index.tsx`**

```tsx
import type { ReactNode } from 'react'
import Link from '@docusaurus/Link'
import useDocusaurusContext from '@docusaurus/useDocusaurusContext'
import Layout from '@theme/Layout'
import styles from './index.module.css'

function Hero() {
  return (
    <div className={styles.hero}>
      <div className={styles.heroInner}>
        <h1 className={styles.heroTitle}>Stash</h1>
        <p className={styles.heroSubtitle}>
          macOS 剪切板管理器 + 密码保险箱
          <br />
          本地存储，隐私优先，免订阅
        </p>
        <div className={styles.heroActions}>
          <Link
            className="button button--primary button--lg"
            to="https://github.com/perseveringman/Stash/releases/latest"
          >
            免费下载
          </Link>
          <Link
            className="button button--secondary button--lg"
            to="/docs/installation"
          >
            查看文档
          </Link>
        </div>
        <p className={styles.heroBadge}>仅支持 macOS · Apple Silicon 和 Intel 均可</p>
      </div>
    </div>
  )
}

type FeatureItem = {
  title: string
  emoji: string
  description: string
}

const features: FeatureItem[] = [
  {
    title: '剪切板历史',
    emoji: '📋',
    description: '自动记录所有复制内容，支持文本、图片、代码、颜色、URL 等类型，随时找回历史记录。',
  },
  {
    title: '智能搜索',
    emoji: '🔍',
    description: '模糊搜索快速定位历史内容，支持按类型、来源应用、标签筛选，键盘全程操作。',
  },
  {
    title: '密码保险箱',
    emoji: '🔐',
    description: 'AES-256 加密存储密码和敏感信息，支持 Touch ID 解锁、TOTP 两步验证码生成。',
  },
  {
    title: '快捷键召唤',
    emoji: '⚡',
    description: '按 Shift+Cmd+V 即可唤起剪切板面板，不打断工作流，数字键 1-9 快速粘贴。',
  },
  {
    title: '代码高亮预览',
    emoji: '✨',
    description: '使用 VS Code 引擎（Shiki）对代码片段语法高亮，JSON 自动格式化，颜色可视化预览。',
  },
  {
    title: '本地隐私优先',
    emoji: '🛡️',
    description: '所有数据存储在本机 SQLite，不上传任何内容。支持可选的 iCloud Drive 多设备同步。',
  },
]

function FeatureCard({ title, emoji, description }: FeatureItem) {
  return (
    <div className={styles.featureCard}>
      <div className={styles.featureEmoji}>{emoji}</div>
      <h3 className={styles.featureTitle}>{title}</h3>
      <p className={styles.featureDesc}>{description}</p>
    </div>
  )
}

export default function Home(): ReactNode {
  const { siteConfig } = useDocusaurusContext()
  return (
    <Layout title={siteConfig.title} description={siteConfig.tagline}>
      <main>
        <Hero />
        <section className={styles.features}>
          <div className={styles.featuresInner}>
            <h2 className={styles.featuresTitle}>核心功能</h2>
            <div className={styles.featuresGrid}>
              {features.map((f) => (
                <FeatureCard key={f.title} {...f} />
              ))}
            </div>
          </div>
        </section>
      </main>
    </Layout>
  )
}
```

**Step 2: 创建 `website/src/pages/index.module.css`**

```css
.hero {
  padding: 5rem 2rem;
  text-align: center;
  background: linear-gradient(135deg, #1a1a2e 0%, #16213e 50%, #0f3460 100%);
  color: white;
}

.heroInner {
  max-width: 800px;
  margin: 0 auto;
}

.heroTitle {
  font-size: 4rem;
  font-weight: 800;
  margin-bottom: 1rem;
  background: linear-gradient(90deg, #a78bfa, #60a5fa);
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
}

.heroSubtitle {
  font-size: 1.4rem;
  color: #cbd5e1;
  margin-bottom: 2rem;
  line-height: 1.8;
}

.heroActions {
  display: flex;
  gap: 1rem;
  justify-content: center;
  flex-wrap: wrap;
  margin-bottom: 1.5rem;
}

.heroBadge {
  font-size: 0.85rem;
  color: #94a3b8;
}

.features {
  padding: 4rem 2rem;
}

.featuresInner {
  max-width: 1100px;
  margin: 0 auto;
}

.featuresTitle {
  text-align: center;
  font-size: 2rem;
  font-weight: 700;
  margin-bottom: 2.5rem;
}

.featuresGrid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
  gap: 1.5rem;
}

.featureCard {
  padding: 1.5rem;
  border-radius: 12px;
  border: 1px solid var(--ifm-color-emphasis-200);
  background: var(--ifm-card-background-color);
  transition: transform 0.2s, box-shadow 0.2s;
}

.featureCard:hover {
  transform: translateY(-3px);
  box-shadow: 0 8px 24px rgba(0, 0, 0, 0.15);
}

.featureEmoji {
  font-size: 2rem;
  margin-bottom: 0.75rem;
}

.featureTitle {
  font-size: 1.1rem;
  font-weight: 600;
  margin-bottom: 0.5rem;
}

.featureDesc {
  font-size: 0.9rem;
  color: var(--ifm-color-emphasis-700);
  line-height: 1.6;
}
```

**Step 3: 验证本地首页正常显示**

```bash
cd website && npm run start
```

打开 `http://localhost:3000` 应看到 Hero 区域和特性卡片。

**Step 4: 提交**

```bash
cd ..
git add website/src/pages/
git commit -m "feat(docs): add product landing page with hero and feature cards"
```

---

### Task 5: 编写安装指南文档

**Files:**
- Create: `website/docs/installation.md`
- Delete: `website/docs/intro.md`（脚手架生成的示例文件，用不到）

**Step 1: 删除示例文档**

```bash
rm website/docs/intro.md
# 如果还有其他脚手架示例文档（tutorial-*.md），一并删除
rm -f website/docs/tutorial-*.md
```

**Step 2: 创建 `website/docs/installation.md`**

```markdown
---
id: installation
title: 安装指南
sidebar_position: 1
---

# 安装指南

## 系统要求

- macOS 12 Monterey 或更高版本
- Apple Silicon（M1/M2/M3）或 Intel 处理器

## 下载安装

### 方式一：从 GitHub Releases 下载（推荐）

1. 访问 [GitHub Releases](https://github.com/perseveringman/Stash/releases/latest) 页面
2. 根据您的处理器架构下载对应版本：
   - **Apple Silicon（M 系列芯片）**：下载 `stash-x.x.x-arm64.dmg`
   - **Intel 处理器**：下载 `stash-x.x.x-x64.dmg`
3. 打开下载的 `.dmg` 文件
4. 将 **Stash** 图标拖入 **Applications** 文件夹

### 如何判断处理器类型

点击左上角苹果菜单 → **关于本机**，查看「芯片」或「处理器」字段。

## 首次启动

1. 打开 **Launchpad** 或 **Applications** 文件夹，点击 **Stash** 启动
2. macOS 可能提示「无法验证开发者」，请按以下步骤处理：
   - 打开 **系统设置** → **隐私与安全性**
   - 在底部找到「Stash 已被阻止」，点击 **仍然打开**
3. 启动后会进入引导页面，按提示完成初始设置

## 授予权限

Stash 需要以下权限才能正常工作：

| 权限 | 用途 | 是否必须 |
|---|---|---|
| 辅助功能（Accessibility） | 自动粘贴到其他应用 | 必须 |
| 输入监控（Input Monitoring） | 监听全局快捷键 | 必须 |

首次触发相关功能时，macOS 会弹出权限申请弹窗，点击「打开系统设置」后在对应权限列表中勾选 **Stash**。

## 开机自启

在 Stash 的「设置」页面可以开启「登录时启动」，Stash 会在菜单栏静默运行，不会弹出主窗口。

## 卸载

将 `/Applications/Stash.app` 移入废纸篓即可。用户数据（剪切板历史、密码保险箱）存储在：

```
~/Library/Application Support/Stash/
```

如需彻底清除数据，请同时删除该目录。
```

**Step 3: 提交**

```bash
git add website/docs/installation.md
git add -u website/docs/  # 暂存删除操作
git commit -m "feat(docs): add installation guide"
```

---

### Task 6: 编写剪切板管理指南

**Files:**
- Create: `website/docs/clipboard.md`

**Step 1: 创建 `website/docs/clipboard.md`**

```markdown
---
id: clipboard
title: 剪切板管理
sidebar_position: 2
---

# 剪切板管理

## 基本使用

Stash 在后台自动监控剪切板，每次您按下 **Cmd+C** 复制内容时，Stash 都会将其保存到历史记录中。

### 唤起面板

按 **Shift+Cmd+V** 唤起剪切板面板。

### 选择并粘贴

| 操作 | 说明 |
|---|---|
| 鼠标点击 | 选中该条目并粘贴到上一个活跃窗口 |
| 数字键 1-9 | 快速粘贴对应位置的条目 |
| 方向键 ↑↓ | 在列表中移动 |
| Enter | 粘贴当前选中条目 |
| Esc | 关闭面板 |

## 搜索

在面板顶部输入框中输入关键词即可进行模糊搜索，支持搜索内容文本。

## 内容类型

Stash 会自动识别并标注以下内容类型：

| 类型 | 说明 |
|---|---|
| 文本 | 普通文字 |
| 链接 | 以 `http://` 或 `https://` 开头的 URL |
| 代码 | 代码片段（自动语法高亮） |
| JSON | JSON 数据（自动格式化预览） |
| 颜色 | HEX/RGB 颜色值（显示色块预览） |
| Base64 | Base64 编码字符串 |
| 图片 | 截图或复制的图片 |
| 文件路径 | 文件系统路径 |

## 收藏与置顶

- **收藏**：悬停在条目上，点击星形图标，收藏的条目不会被自动清除
- **置顶**：点击图钉图标，置顶的条目始终显示在列表顶部

## 序列粘贴

序列粘贴允许您将多个条目排队，按顺序依次粘贴：

1. 在剪切板面板中，依次选择多个条目并标记为序列
2. 每次按 **Shift+Cmd+V** 触发粘贴时，自动粘贴队列中的下一条

## 批量粘贴

选择多个条目后，可将它们合并为一段文字一次性粘贴。

## 模板片段

在「设置 → 模板」中可以保存常用文字模板（如邮件签名、代码片段），在面板中通过 `/` 触发快速插入。

## iCloud 同步

在「设置 → 通用」中开启 **iCloud Drive 同步**，剪切板历史将同步到您的所有 Mac 设备。

> **注意**：同步仅包含文本类型历史，图片不会同步。

## 清除历史

在「设置 → 隐私」中可以手动清除全部历史记录，或设置自动清除策略（如保留最近 30 天）。
```

**Step 2: 提交**

```bash
git add website/docs/clipboard.md
git commit -m "feat(docs): add clipboard management guide"
```

---

### Task 7: 编写密码保险箱指南

**Files:**
- Create: `website/docs/vault.md`

**Step 1: 创建 `website/docs/vault.md`**

```markdown
---
id: vault
title: 密码保险箱
sidebar_position: 3
---

# 密码保险箱

## 初始化保险箱

首次使用保险箱前需要设置主密码：

1. 按 **Shift+Cmd+P** 打开保险箱，或从菜单栏图标进入
2. 点击「设置主密码」
3. 输入一个强密码（建议 12 位以上，包含大小写字母、数字、符号）
4. 可选：设置密码提示问题（仅显示提示，不保存答案）
5. 妥善保存显示的「恢复密钥」，这是忘记主密码时唯一的找回方式

> ⚠️ **重要**：主密码无法找回。若遗失主密码且无恢复密钥，保险箱数据将永久无法访问。

## 解锁保险箱

设置主密码后，每次进入保险箱时需要验证身份：

- **Touch ID**（若 Mac 支持并已启用）：按提示触摸指纹传感器
- **主密码**：手动输入主密码

在「设置 → 安全」中可以选择：
- **宽松模式**：Touch ID 失败后自动回退到密码输入
- **严格模式**：必须输入主密码，不接受 Touch ID

## 添加密码条目

1. 解锁保险箱后，点击右上角 **+** 按钮
2. 填写以下信息：
   - 标题（如「Gmail」「公司 VPN」）
   - 用户名 / 邮箱
   - 密码（可点击「生成密码」自动生成强密码）
   - 网址（可选）
   - 备注（可选）
3. 点击「保存」

## 密码生成器

在添加/编辑条目时，点击密码字段旁的「生成」按钮，可以：

- 设置密码长度（默认 16 位）
- 选择包含大写字母、小写字母、数字、特殊符号

## TOTP 两步验证码

若某账户启用了 TOTP 两步验证（如 Google Authenticator 等），可以在条目中添加 TOTP 密钥：

1. 编辑密码条目，找到「两步验证」字段
2. 扫描二维码或手动输入密钥（通常以 `base32` 格式提供）
3. 保存后，该条目将显示实时动态验证码（每 30 秒更新）

## 自动填写（Auto-Type）

Stash 可以自动将用户名和密码填入当前激活窗口：

1. 在保险箱中找到对应条目
2. 点击「自动填写」或使用对应快捷键
3. Stash 会自动切换到上一个活跃窗口并依次输入用户名、Tab、密码

> **注意**：自动填写需要授予「辅助功能」权限。

## 自动锁定

保险箱在以下情况自动锁定：

- 屏幕保护程序启动或屏幕锁定
- 窗口失去焦点（严格模式下）
- 设置的超时时间到达（在「设置 → 安全」中配置，默认 5 分钟）

## 安全审计日志

在「设置 → 安全 → 审计日志」中可以查看所有保险箱访问记录（解锁时间、操作类型），帮助发现异常访问。
```

**Step 2: 提交**

```bash
git add website/docs/vault.md
git commit -m "feat(docs): add password vault guide"
```

---

### Task 8: 编写快捷键参考

**Files:**
- Create: `website/docs/shortcuts.md`

**Step 1: 创建 `website/docs/shortcuts.md`**

```markdown
---
id: shortcuts
title: 快捷键参考
sidebar_position: 4
---

# 快捷键参考

## 全局快捷键

这些快捷键在任何应用程序中均可使用（需要「输入监控」权限）：

| 快捷键 | 功能 |
|---|---|
| **Shift+Cmd+V** | 唤起 / 隐藏剪切板面板 |
| **Shift+Cmd+P** | 打开密码保险箱 |
| **Alt+W** | 显示 / 隐藏小组件（Mini Widget） |
| **Alt+1 ~ Alt+5** | 快速粘贴小组件中的第 1-5 条目 |

> 全局快捷键可在「设置 → 快捷键」中自定义。

## 剪切板面板内快捷键

当剪切板面板处于焦点时有效：

| 快捷键 | 功能 |
|---|---|
| **1 ~ 9** | 快速粘贴对应位置的条目 |
| **↑ / ↓** | 在列表中上下移动 |
| **Enter** | 粘贴当前选中条目 |
| **Esc** | 关闭面板 |
| **Cmd+F** | 将光标移到搜索框 |
| **Delete** | 删除当前选中条目 |

## 密码保险箱内快捷键

| 快捷键 | 功能 |
|---|---|
| **Cmd+N** | 新建密码条目 |
| **Cmd+C**（在条目上） | 复制密码到剪切板 |
| **Cmd+B**（在条目上） | 复制用户名到剪切板 |
| **Esc** | 关闭保险箱 / 取消编辑 |
```

**Step 2: 提交**

```bash
git add website/docs/shortcuts.md
git commit -m "feat(docs): add keyboard shortcuts reference"
```

---

### Task 9: 编写 FAQ

**Files:**
- Create: `website/docs/faq.md`

**Step 1: 创建 `website/docs/faq.md`**

```markdown
---
id: faq
title: FAQ
sidebar_position: 5
---

# 常见问题

## 安装与启动

### Stash 打不开，提示「无法验证开发者」

这是 macOS Gatekeeper 的安全机制。解决方法：

1. 打开 **系统设置 → 隐私与安全性**
2. 向下滚动找到「已阻止使用 Stash，因为来自未知开发者」
3. 点击 **仍然打开**，再次确认

### Stash 无法粘贴到其他应用

请检查「辅助功能」权限是否已授予：

1. **系统设置 → 隐私与安全性 → 辅助功能**
2. 确认列表中有 **Stash** 并已打开开关
3. 若不在列表，点击左下角 **+** 号手动添加

### 快捷键 Shift+Cmd+V 没有反应

请检查「输入监控」权限：

1. **系统设置 → 隐私与安全性 → 输入监控**
2. 确认 **Stash** 已被勾选
3. 权限更改后可能需要重启 Stash

## 剪切板功能

### 为什么某些内容没有被记录？

Stash 不记录以下内容（出于隐私和安全考虑）：

- 密码管理器（1Password、Bitwarden 等）复制的内容（这些应用会标记剪切板来源为敏感）
- 长度超过 1MB 的内容

### 如何防止某些应用的内容被记录？

在「设置 → 隐私 → 排除应用」中添加不想被监控的应用。

### 历史记录保存多久？

默认保存最近 **500 条**记录。可在「设置 → 通用」中调整保存数量或设置按时间清除。

## 密码保险箱

### 忘记了主密码怎么办？

在初始化保险箱时保存的「恢复密钥」是唯一找回途径。使用恢复密钥可以重置主密码。

如果同时丢失了主密码和恢复密钥，保险箱数据将**无法恢复**。

### 保险箱数据存在哪里？

存储在本机：`~/Library/Application Support/Stash/vault.db`，使用 AES-256 加密，不会上传到任何服务器。

### 更换 Mac 后能迁移数据吗？

可以。将以下目录完整复制到新 Mac 同样路径即可：

```
~/Library/Application Support/Stash/
```

## 其他

### Stash 会影响系统性能吗？

Stash 使用轮询方式监控剪切板（每 500ms 检查一次），CPU 占用极低，常驻内存约 50-80MB。

### 如何彻底卸载？

1. 将 `/Applications/Stash.app` 移入废纸篓
2. 删除用户数据目录：`~/Library/Application Support/Stash/`
3. 清空废纸篓
```

**Step 2: 提交**

```bash
git add website/docs/faq.md
git commit -m "feat(docs): add FAQ page"
```

---

### Task 10: 编写更新日志

**Files:**
- Create: `website/docs/changelog.md`

**Step 1: 创建 `website/docs/changelog.md`**

```markdown
---
id: changelog
title: 更新日志
sidebar_position: 6
---

# 更新日志

## v1.0.1 · 2025-xx-xx

### 修复
- 修复自动更新错误日志和定时器清理问题

### 改进
- 集成 electron-updater 自动更新，应用可在后台静默检查并安装新版本

---

## v1.0.0 · 首次发布

- 剪切板历史管理（文本、图片、代码、URL、颜色、JSON、Base64 等类型）
- 全局快捷键 Shift+Cmd+V 唤起面板
- 模糊搜索 + 数字键快速粘贴
- 收藏与置顶
- 序列粘贴 / 批量粘贴
- 模板片段管理
- 密码保险箱（AES-256 加密）
- Touch ID 解锁
- TOTP 两步验证码生成
- 密码生成器
- 自动填写（Auto-Type）
- 小组件（Mini Widget）
- iCloud Drive 同步
- 系统托盘集成
- 深色 / 浅色主题
```

**Step 2: 提交**

```bash
git add website/docs/changelog.md
git commit -m "feat(docs): add changelog page"
```

---

### Task 11: 创建 GitHub Actions 部署 Workflow

**Files:**
- Create: `.github/workflows/docs.yml`

**Step 1: 创建 `.github/workflows/docs.yml`**

```yaml
name: Deploy Docs to GitHub Pages

on:
  push:
    branches:
      - main
    paths:
      - 'website/**'

  # 允许手动触发
  workflow_dispatch:

permissions:
  contents: write

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm'
          cache-dependency-path: website/package-lock.json

      - name: Install dependencies
        run: npm ci
        working-directory: website

      - name: Build Docusaurus site
        run: npm run build
        working-directory: website

      - name: Deploy to GitHub Pages
        uses: peaceiris/actions-gh-pages@v4
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./website/build
          publish_branch: gh-pages
```

**Step 2: 提交**

```bash
git add .github/workflows/docs.yml
git commit -m "ci: add GitHub Actions workflow to deploy docs to GitHub Pages"
```

---

### Task 12: 本地构建验证 + 检查占位资源

**Files:**
- Create: `website/static/img/logo.png`（临时占位，须替换为真实 logo）

**Step 1: 检查构建是否通过**

```bash
cd website && npm run build
```

预期：输出 `[SUCCESS] Generated static files in "build".`，无 Error。

如果报错「找不到 logo.png」，在 `docusaurus.config.ts` 中临时注释掉 logo 配置：

```typescript
// logo: {
//   alt: 'Stash Logo',
//   src: 'img/logo.png',
// },
```

**Step 2: 检查生成的文件**

```bash
ls website/build/
ls website/build/docs/
```

应包含：`index.html`、`docs/` 目录（含 installation、clipboard、vault、shortcuts、faq、changelog 各页面）。

**Step 3: 在浏览器预览构建产物**

```bash
npm run serve
```

打开 `http://localhost:3000/z-paste/` 验证首页和各文档页面正常显示。

**Step 4: 最终提交**

```bash
cd ..
git add -A
git commit -m "docs: finalize documentation site ready for GitHub Pages deployment"
```

---

## 部署后配置（人工操作，一次性）

构建 workflow 推送到 `gh-pages` 分支后，需要在 GitHub 仓库设置中配置 Pages：

1. 进入 GitHub 仓库 → **Settings → Pages**
2. **Source** 选择 `Deploy from a branch`
3. **Branch** 选择 `gh-pages`，目录选 `/ (root)`
4. 保存，等待部署完成

文档站将发布到：`https://perseveringman.github.io/z-paste/`

---

## 注意事项

- `website/static/img/logo.png`：需要提供真实的应用图标（可从 `resources/icon.icns` 导出 PNG）
- `changelog.md` 中的日期需要根据实际发布日期更新
- 若仓库启用了分支保护，`GITHUB_TOKEN` 需要有 `contents: write` 权限（workflow 中已配置）
